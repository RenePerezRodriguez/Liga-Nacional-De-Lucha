rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Public Read
      allow read: if true;
      
      // Standard Auth Write (User must be logged in)
      allow write: if request.auth != null;
    }
  }
}
